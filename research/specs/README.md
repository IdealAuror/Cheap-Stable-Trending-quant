# research/specs/

存放每轮 `research-spec`（由高级模型按 `docs/prompts/01-research-spec-design.md` 产出）。

## 命名规范

见 `research/_index.md` 第一节。文件名：`<spec_id>.md`，如 `P1-F1-EV-2026Q2-v1.md`。

## 修订规则

- spec 一经 `locked`，**预注册的通过标准不可调整**。
- 其他字段修订 → 生成新版本号（v1→v2），旧版本保留。
- 涉及通过标准的修订 → 视为新一轮假设，重置迭代计数器。
